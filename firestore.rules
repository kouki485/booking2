rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // 開発段階のため、一時的にすべてのアクセスを許可
    // 本番運用時は適切なセキュリティルールに変更してください
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 